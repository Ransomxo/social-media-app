{
  "version": 2,
  "buildCommand": "cd packages/frontend && pnpm install --no-frozen-lockfile && pnpm run build",
  "outputDirectory": "packages/frontend/.next",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "framework": "nextjs"
}
