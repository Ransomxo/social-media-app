{
  "version": 2,
  "buildCommand": "cd ../.. && pnpm install && pnpm turbo run build --filter=@social-media-app/backend...",
  "installCommand": "cd ../.. && pnpm install",
  "outputDirectory": "dist",
  "builds": [
    {
      "src": "dist/index.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "dist/index.js"
    }
  ],
  "env": {
    "DATABASE_URL": "@database-url",
    "JWT_SECRET": "@jwt-secret",
    "NEXT_PUBLIC_APP_URL": "@next-public-app-url"
  }
}
