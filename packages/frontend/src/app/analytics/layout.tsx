import React from 'react';

export const metadata = {
  title: 'Analytics - Omniposting',
  description: 'View your social media analytics and insights',
};

export default function AnalyticsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
